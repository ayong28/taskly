# Handoff — taskly

**Date:** 2026-07-20
**Repo:** `/Users/adrian/workspace/2026-projects/taskly` (renamed this session — was `project-manager-01`)
**Branch:** `master`

## Where things stand

Renamed the local repo directory from `project-manager-01` to `taskly` to
match the app's actual name (the GitHub remote, `package.json`'s `name`
field, and the visual branding have all said "Taskly" since much earlier
sessions — the folder name was the one remaining leftover).

- **GitHub was unaffected**: the remote is `github.com/ayong28/taskly.git`,
  already named correctly, and has no dependency on the local folder name.
- **No implementation code referenced the old absolute path** — verified by
  grep before renaming; every config (`.mcp.json`, `playwright.config.ts`,
  `jest.config.ts`, `package.json`) already resolves paths relative to
  `process.cwd()`/the repo root, which is exactly why this was safe.
- **Two things outside the repo did hardcode the old path, both fixed**:
  - `~/.hermes/config.yaml`'s `taskly` MCP server entry (`cd
    /Users/adrian/workspace/2026-projects/project-manager-01 && ...`) →
    updated to the new path.
  - `.claude/settings.local.json` (gitignored, local-only) — several
    pre-approved Bash allowlist entries hardcoded the old path (mostly
    dead/superseded by the broader `Bash(git *)` entry already in there) —
    updated the path strings for accuracy anyway.
- **In-repo docs updated**: `project-manager-plan.md`'s Step 17 directory
  diagram (`project-manager-01/` → `taskly/`), and
  `docs/mcp-server-setup.md`'s placeholder path (`/path/to/project-manager-01`
  → `/path/to/taskly`, so the placeholder itself doesn't reference a
  directory name that no longer exists).
- **Deliberately left untouched**: the five earlier dated handoff docs
  (`2026-07-06.md` through `2026-07-19-mcp-workspace-extraction.md`) still
  say `project-manager-01` in their headers — those are historical journal
  entries, accurate for what was true in those sessions. Rewriting them to
  say "taskly" would misrepresent what the repo was actually called at the
  time.
- Verified `tsc --noEmit` and the full Jest suite pass from the new path
  before touching any docs.

## Commits this session

(to be filled in once committed — see git log)

## What's left

- Nothing outstanding from the rename itself — repo, GitHub remote, MCP
  configs (in-repo and Hermes), and docs are all consistent.
- Everything previously listed as "What's left" in the
  `2026-07-19-mcp-workspace-extraction.md` handoff (the pre-existing
  `archive.spec.ts` flake, merge/push review) is still open — this session
  only touched the rename.
